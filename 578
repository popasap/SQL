select question_id as survey_log from survey_log group by question_id having count(answer_id) =
(select max(count_num) from
(select count(answer_id) as count_num from survey_log group by question_id) x)
